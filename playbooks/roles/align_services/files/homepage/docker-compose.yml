services:
  homepage:
    restart: unless-stopped
    image: gethomepage/homepage:v1.13.2
    container_name: homepage
    networks:
      - services
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock # (optional) For docker integrations, see alternative methods
      - /mnt/containers/homepage/images:/app/public/images
      - /mnt/containers/homepage/etc:/app/config
    environment:
      HOMEPAGE_ALLOWED_HOSTS: ${HOST} # required, may need port. See gethomepage.dev/installation/#homepage_allowed_hosts
    labels:

      - "traefik.enable=true"
      - "traefik.http.routers.homepage.rule=Host(`${HOST}`)"
      - "traefik.http.services.homepage.loadbalancer.server.port=3000"
      - "traefik.http.routers.homepage.entrypoints=websecure"
      - "traefik.http.routers.homepage.tls=true"
      - "traefik.http.routers.homepage.tls.certresolver=myresolver"

networks:
  services:
    name: services
    external: true
