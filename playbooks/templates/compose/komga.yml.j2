
---
services:
  komga:
    image: gotson/komga:latest
    container_name: komga
    ports: {{ app_ports }}
    restart: unless-stopped
    volumes:
      - {{ komga_config }}:/config
      - {{ komga_data }}:/data
      - type: bind
        source: /etc/timezone
        target: /etc/timezone
        read_only: true
